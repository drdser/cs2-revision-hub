import type { ChapterInfo } from '../types';

/**
 * Chapter notes.
 *
 * Content is drawn from the lecture decks supplied for the course. Chapters 12,
 * 13 and 14 have `covered: false` because no lecture material for them was
 * provided and no question on the review sheet or on any previous final maps to
 * them. Nothing has been invented to fill those gaps.
 */
export const chapters: ChapterInfo[] = [
  {
    chapter: 9,
    title: 'Inheritance',
    covered: true,
    summary: [
      'Inheritance is a form of software reuse: a new class acquires the members of an existing class and adds capabilities of its own. The existing class is the superclass, the new one is the subclass.',
      'A subclass is more specific than its superclass, which is why inheritance is sometimes called specialization. A subclass can itself become the superclass of future subclasses.',
      'The direct superclass is the one named after extends. Anything higher in the hierarchy is an indirect superclass. Java supports single inheritance only: each class has exactly one direct superclass.',
      'Every class in Java directly or indirectly extends Object in package java.lang. A class that does not name a superclass extends Object implicitly, which is where toString and getClass come from.',
      'Inheritance is the is-a relationship. Composition is the has-a relationship: an Employee is not a BirthDate, but an Employee has a BirthDate.',
      'public members are accessible everywhere a reference is available. private members are accessible only inside the superclass itself. protected sits between the two: accessible in the superclass and its subclasses.',
      'Each subclass constructor must call a superclass constructor with super(...) as its first statement. If it does not, the compiler inserts a call to the superclass no-argument constructor, which is an error when no such constructor exists.',
      'To override a method, the subclass declares one with the same signature. The superclass version stays reachable through super.methodName().',
    ],
    keywords: [
      'extends',
      'super',
      'super(...)',
      'protected',
      'private',
      'public',
      '@Override',
      'toString',
      'Object',
      'is-a',
      'has-a',
    ],
    rules: [
      'super(...) must be the FIRST statement in a subclass constructor.',
      'Constructors finish from the top of the hierarchy downwards: the superclass constructor body completes before the subclass constructor body.',
      'A method signature is the name plus the number, type and order of the parameters. The return type is not part of it.',
      'It is a compilation error to override a method with a more restricted access modifier.',
      'Assigning a subclass reference to a superclass variable is safe. The reverse is a compilation error.',
      'A superclass private member cannot be used by name inside a subclass; reach it through an inherited public or protected method.',
      'public and protected members keep their original access level when inherited.',
    ],
    mistakes: [
      'Putting super(...) after another statement in the constructor. It must come first.',
      'Reading a private superclass field directly in a subclass method, which does not compile.',
      'Writing toString() instead of super.toString() inside an overriding toString, which recurses forever.',
      'Assuming inherited fields are re-initialised by the subclass constructor. If the subclass assigns them, it overwrites what the superclass constructor set.',
      'Forgetting that a double prints with a .0 suffix, so 500 comes out as 500.0.',
    ],
    traps: [
      'Only public methods let a subclass reach private superclass variables. FALSE: protected methods work too.',
      'A subclass constructor without super(...) always compiles. FALSE: only if the superclass has a no-argument or default constructor.',
      'Superclass variable = subclass object is fine; subclass variable = superclass object is a compilation error.',
      'print versus println. A superclass method using print leaves the cursor on the line, so the subclass text continues it.',
    ],
  },
  {
    chapter: 10,
    title: 'Polymorphism & Interfaces',
    covered: true,
    summary: [
      'Polymorphism means having many forms. The same message sent to different objects produces different results, because each object responds in a way appropriate to its own type.',
      'When a superclass variable holds a reference to a subclass object and calls an overridden method, the subclass version runs. Choosing that version at execution time is called dynamic binding, or late binding.',
      'A superclass reference can invoke only methods declared in the superclass. Reaching a subclass-only member needs a downcast, normally guarded with instanceof.',
      'An abstract class cannot be instantiated. It exists to be extended and normally declares one or more abstract methods, which have no body. Any class containing an abstract method must be declared abstract.',
      'A concrete class provides an implementation for every method it declares, including every abstract method inherited from its superclass.',
      'Constructors and static methods cannot be abstract. Abstract superclass variables can still be declared, and are what makes polymorphic processing possible.',
      'An interface declaration contains only constants and abstract methods. Its methods are implicitly public and abstract, and its fields are implicitly public, static and final.',
      'A class uses an interface with implements, and must supply every method the interface declares, or be declared abstract. A class may implement any number of interfaces but extend only one superclass.',
      'Use an interface when unrelated classes need a common set of methods. Use an abstract class when related classes also share instance variables and concrete method bodies.',
      'final methods cannot be overridden and final classes cannot be extended. private and static methods are implicitly final, so their calls use static binding.',
    ],
    keywords: [
      'abstract',
      'interface',
      'implements',
      'extends',
      'instanceof',
      'final',
      'getClass()',
      'getName()',
      'public static final',
      'public abstract',
      '@Override',
    ],
    rules: [
      'Attempting to instantiate an abstract class is a compilation error.',
      'A class containing an abstract method must itself be declared abstract, even if it has concrete methods too.',
      'A class that does not implement every interface method must be declared abstract.',
      'Interface fields are constants. Assigning to one inside an implementing class does not compile.',
      'Interface methods must be declared public in the implementing class, because they are implicitly public.',
      'Multiple interfaces are listed comma-separated after implements.',
      'Dynamic binding follows the object, not the declared type of the variable.',
      'Calls to final, static and private methods are resolved at compile time (static binding).',
    ],
    mistakes: [
      'Trying to call a subclass-only method through a superclass variable without a cast.',
      'Casting without an instanceof guard, which risks a ClassCastException.',
      'Assuming the superclass version of an overridden method runs because the variable has the superclass type. It does not.',
      'Thinking a class can extend more than one class. It cannot; only interfaces can be combined freely.',
      'Confusing overloading with overriding. Overloading changes the parameter list and is resolved at compile time.',
    ],
    traps: [
      '"Choose the wrong statement" questions about interfaces, where every option is actually correct and the answer is "none of the above".',
      'An interface constant printed as 0.10 shows as 0.1, because Java drops the trailing zero on a double.',
      'A method returning double prints 0 as 0.0 and 1200 as 1200.0.',
      'An abstract superclass method that calls its own abstract method still dispatches to the subclass implementation.',
      'A variable of an interface type only exposes the methods declared in THAT interface, even if the object implements others.',
    ],
  },
  {
    chapter: 11,
    title: 'Exception Handling',
    covered: true,
    summary: [
      'An exception indicates a problem that occurs while a program executes. Without exception handling the program terminates as soon as the problem occurs; with it, the program can continue, which makes it fault-tolerant.',
      'The try block holds code that might throw. A catch block, also called an exception handler, catches and handles one exception type. The finally block runs whether or not an exception was thrown, and normally holds resource-release code.',
      'A try block must be followed by at least one catch block and/or a finally block. So try/catch, try/finally and try/catch/finally are all valid.',
      'If the bodies of several catch blocks would be identical, multi-catch (Java SE 7) combines them: catch (Type1 | Type2 e). The types are separated by a vertical bar.',
      'Java uses the termination model: when an exception occurs the try block terminates immediately, control transfers to the first matching catch block, and afterwards execution resumes after the last catch block.',
      'Local variables declared in a try block go out of scope when it terminates, so they are not visible in the catch blocks.',
      'The throws clause appears in the method heading, after the parameter list, and lists the exception types the method might throw, comma-separated. throw, without the s, is the statement that raises an exception object.',
      'A user-defined exception must extend Exception. Its constructor normally passes the message to the superclass with super(s), which is what getMessage() later returns.',
      'The stack trace records every method invocation from the start of the program to the point where the exception was generated. printStackTrace() prints it; getStackTrace() returns a StackTraceElement array.',
      'A precondition must be true when a method is invoked; a postcondition must be true after it successfully returns. Both can be specified informally as comments before the method declaration.',
      'An assertion is a boolean expression that should be true at a given point unless there is a bug. Java has two forms of the assert statement, and both throw an AssertionError when the condition is false.',
      'try-with-resources (Java SE 7) releases resources automatically. Each resource must implement AutoCloseable, and multiple resources are separated by semicolons.',
    ],
    keywords: [
      'try',
      'catch',
      'finally',
      'throw',
      'throws',
      '|',
      'assert',
      'AutoCloseable',
      'Exception',
      'AssertionError',
      'printStackTrace()',
      'getMessage()',
      'getStackTrace()',
      '-ea',
    ],
    rules: [
      'A try block cannot stand alone. It needs a catch block, a finally block, or both.',
      'finally always runs, including when the try block exits with return or break. Only System.exit skips it.',
      'Catch blocks are checked in order and only the first match runs, so list the more specific type before the more general one.',
      'Multi-catch separates its types with |. A throws clause separates its types with commas.',
      'A method that throws an exception terminates without returning a value.',
      'A custom exception class must extend Exception.',
      'Java has exactly 2 versions of the assert statement.',
      'Assertions are disabled by default. Enable them with java -ea.',
      'AssertionError is a subclass of Error, not of Exception.',
      'Exception handling is for synchronous errors, not asynchronous events.',
    ],
    mistakes: [
      'Swapping the roles of try and catch in a description. The try block contains the risky code.',
      'Expecting execution to resume where the exception happened. Java uses the termination model.',
      'Declaring a variable inside try and then using it in catch, which does not compile.',
      'Writing throw where throws belongs, or the reverse.',
      'Forgetting that assertions do nothing unless -ea is passed, which can hide the bug the assertion was meant to catch.',
      'Using a comma instead of a vertical bar in multi-catch.',
    ],
    traps: [
      'try-with-resources and multi-catch were introduced in Java SE 7, not SE 6.',
      'The exact message for integer division by zero is "/ by zero".',
      'An exception thrown while building a println argument means NOTHING from that line is printed.',
      'A finally block in a try statement with no matching catch still runs before the exception propagates outwards.',
      'A finally block runs even after a return has computed its value.',
    ],
  },
  {
    chapter: 12,
    title: 'Not covered by the supplied material',
    covered: false,
    coverageNote:
      'Source coverage for this chapter is limited. No lecture deck for Chapter 12 was supplied, and no question on the current review sheet or on any previous final maps to it. Nothing has been invented to fill the gap. Check with your instructor whether this chapter is examinable.',
    summary: [],
    keywords: [],
    rules: [],
    mistakes: [],
    traps: [],
  },
  {
    chapter: 13,
    title: 'Not covered by the supplied material',
    covered: false,
    coverageNote:
      'Source coverage for this chapter is limited. No lecture deck for Chapter 13 was supplied, and no question on the current review sheet or on any previous final maps to it. Nothing has been invented to fill the gap. Check with your instructor whether this chapter is examinable.',
    summary: [],
    keywords: [],
    rules: [],
    mistakes: [],
    traps: [],
  },
  {
    chapter: 14,
    title: 'Not covered by the supplied material',
    covered: false,
    coverageNote:
      'Source coverage for this chapter is limited. No lecture deck for Chapter 14 was supplied. The only related material anywhere in the sources is the use of Character.isUpperCase and Character.isLowerCase in the file-processing question on the current review sheet, and those appear in the Chapter 15 questions here. Nothing else has been invented.',
    summary: [],
    keywords: [],
    rules: [],
    mistakes: [],
    traps: [],
  },
  {
    chapter: 15,
    title: 'Files & Streams',
    covered: true,
    summary: [
      'The java.io package contains nearly every class needed to perform input and output. Input and output sources are represented as streams, and a stream is a sequence of data.',
      'Java provides three standard streams: System.in for input (usually the keyboard), System.out for output (usually the screen), and System.err for error output.',
      'Byte streams perform input and output of 8-bit bytes. The classes used most often are FileInputStream and FileOutputStream.',
      'Character streams perform input and output of 16-bit Unicode. The classes used most often are BufferedReader and BufferedWriter.',
      'read() returns the next byte or character as an int, or -1 when the end of the file is reached. That is why the loop is written while ((c = in.read()) != -1).',
      'BufferedReader can read a single character with read() or a whole line with readLine(). readLine() returns null at the end of the stream, not -1.',
      'A BufferedReader object requires a FileReader object; a BufferedWriter object requires a FileWriter object.',
      'PrintWriter prints the formatted representation of objects to a character-output stream, including primitive types such as int and long, and arrays.',
      'Passing true as the second constructor argument to FileWriter or FileOutputStream appends to the file instead of replacing its contents.',
      'Files in the project folder can be named directly. Files elsewhere need an absolute path, and because a backslash begins an escape sequence, it must be written twice: "C:\\\\Users\\\\input.txt".',
      'File.mkdir() creates a directory and returns true on success, false on failure. File.list() returns a String array naming the files and directories in a directory.',
      'Unicode is a 16-bit character set covering the major living and dead languages plus scientific symbols. UTF-8 is used to manipulate Unicode characters in Java; UTF stands for Unicode Transformation Format and the 8 means 8-bit blocks.',
    ],
    keywords: [
      'java.io',
      'FileInputStream',
      'FileOutputStream',
      'FileReader',
      'FileWriter',
      'BufferedReader',
      'BufferedWriter',
      'PrintWriter',
      'File',
      'read()',
      'readLine()',
      'write()',
      'close()',
      'mkdir()',
      'list()',
      'System.in',
      'System.out',
      'System.err',
      'UTF-8',
    ],
    rules: [
      'read() signals end of file with -1. readLine() signals it with null.',
      'read() returns an int, so cast it to char before printing the character itself.',
      'BufferedReader wraps FileReader. BufferedWriter wraps FileWriter.',
      'Always close a stream, or use try-with-resources. Buffered output may never reach the disk otherwise.',
      'FileNotFoundException is a subclass of IOException, so catching IOException covers it.',
      'The two-argument FileWriter constructor appends; the one-argument version overwrites.',
      'mkdir() returns false if the directory already exists.',
      'In a Java String literal, \\\\ produces one backslash.',
    ],
    mistakes: [
      'Testing readLine() against -1, or read() against null. Neither compiles or works.',
      'Printing the int from read() directly, which shows the numeric character code instead of the character.',
      'Forgetting close(), so buffered output is lost.',
      'Declaring counters inside the read loop, so they reset on every character.',
      'Expecting the one-argument FileWriter constructor to append.',
      'Writing a Windows path with single backslashes.',
    ],
    traps: [
      'UTF stands for UNICODE Transformation Format, not "Unified".',
      'The class that writes formatted output is PrintWriter, not BufferedWriter.',
      'The method that creates a directory is mkdir, not makeDir or makeDirectory.',
      'String is in java.lang, not java.io, even though it appears in every file program.',
      'flush() pushes buffered data out but does not close the stream; close() does both.',
      'PrintWriter.print(char[]) writes the characters themselves, so ABCD appears rather than an address.',
    ],
  },
];

export const coveredChapters = chapters.filter((c) => c.covered).map((c) => c.chapter);

export function getChapter(n: number): ChapterInfo | undefined {
  return chapters.find((c) => c.chapter === n);
}
